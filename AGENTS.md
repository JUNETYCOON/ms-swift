# AGENTS.md

本文件为 AI 编码 Agent 在本仓库中工作时提供统一指导，适用于仓库根目录及其所有子目录。
如果子目录中存在更具体的 `AGENTS.md`，则以距离目标文件最近的规范为准。

## 沟通与事实边界

- 与用户沟通、任务说明和新增项目文档默认使用中文；代码标识符、命令、日志以及已有英文文档保持原语言。
- 先区分已验证事实、合理推断和待确认事项。不得虚构代码行为、测试结果、运行环境或性能数据。
- 结论应尽量给出对应的文件、代码路径、命令或测试证据。
- 用户明确要求只读、仅分析或暂不执行时，不修改文件、不安装依赖、不启动任务。
- 当需求存在会实质影响实现的歧义时，先说明不同解释及影响，再请求确认；低风险细节可采用最简单的合理假设，但必须明确说明。

## 项目概述

ms-swift 是面向大模型和多模态大模型的训练、对齐、推理、评测、量化与部署框架。仓库同时覆盖多种模型、训练范式、推理后端和硬件环境，因此修改时应特别关注可选依赖、兼容性边界和回归范围。

开始工作前，应先阅读与任务直接相关的现有实现、测试、示例和文档。以下目录仅作为导航，不替代源码核验：

- `swift/cli/`：命令行入口。
- `swift/arguments/`：参数定义与解析。
- `swift/pipelines/`：训练、推理、导出等流程编排。
- `swift/model/`、`swift/template/`、`swift/dataset/`：模型、模板和数据处理能力。
- `swift/trainers/`、`swift/rlhf_trainers/`：训练器及对齐训练实现。
- `swift/infer_engine/`：推理引擎适配。
- `swift/tuners/`、`swift/tuner_plugin/`：微调方法与扩展。
- `examples/`：可运行示例和推荐参数用法。
- `tests/`：单元测试、冒烟测试与集成测试。
- `docs/source/`、`docs/source_en/`：中英文文档源文件。
- `requirements/`：不同功能和后端的可选依赖。

## 开发前检查

实现前至少完成以下检查：

1. 查看 `git status`，识别并保留用户已有改动。
2. 定位入口、调用链、配置定义、相邻实现和对应测试，避免仅凭文件名或文档猜测行为。
3. 明确输入、输出、兼容性要求、运行环境、可选依赖和验收方式。
4. 对 bug 构造最小稳定复现；对新功能先定义可验证的正常路径和边界情况。
5. 评估是否涉及公共 API、命令行参数、配置序列化、检查点、数据格式或第三方后端兼容性。

对于多步骤任务，先给出简洁计划，每一步同时说明验证方式。任务较小、目标明确且风险较低时，可以直接实施并验证。

## 实现原则

### 简单优先

- 只实现当前需求所必需的代码，不增加未经要求的功能、配置项、兼容层或扩展点。
- 优先复用仓库现有的注册、参数、工厂、插件和工具机制，不重复造轮子。
- 不为一次性逻辑创建抽象层；只有在确实降低复杂度或消除有意义的重复时才抽象。
- 不用复杂的异常处理掩盖需求理解不足或接口边界不清。

### 最小改动

- 每一处修改都应能直接追溯到当前任务。
- 不顺手重构、重命名或重新格式化无关代码，不清理任务开始前已存在的冗余。
- 遵循目标模块现有的命名、组织、日志、异常处理和测试风格。
- 清理本次改动产生的未使用导入、变量、配置和死分支。
- 对上游同步代码、第三方代码和自动生成代码保持克制；优先通过已有扩展点或薄适配层解决问题。

### 兼容性与依赖边界

- 修改公共 API、CLI 参数、配置字段、数据格式或检查点加载逻辑时，必须评估向后兼容性。
- 新增模型、模板、数据集、训练方法或后端支持时，应遵循同类实现的注册方式和文件布局。
- 可选功能不得无条件引入重量级依赖；沿用仓库现有的依赖检测和延迟导入模式。
- 不把某个下游业务、私有环境、机器路径或临时实验约定耦合进通用核心模块。
- 涉及分布式训练、设备选择、精度类型或后端差异时，不假设 CUDA 是唯一环境；只声明实际验证过的平台。

### 错误处理与代码结构

- 遵循快速失败原则。非法输入或不满足的前置条件应尽早暴露，并提供可定位的错误信息。
- 不使用空的或过宽的 `try-except`、静默回退、伪造默认值等方式吞掉错误。
- `hasattr`、动态导入和兼容分支只应用于真实的可选能力探测，不应用于掩盖内部接口不一致。
- 优先使用显式参数和清晰的数据流，避免隐藏的全局状态和跨模块隐含约定。
- 使用守卫式写法降低嵌套；保持函数、类和模块职责集中。
- 依赖方向应清晰，高层编排不应泄漏到低层通用组件，低层组件也不应持有调用方专属知识。
- 文件规模不是硬约束；当文件接近或超过约 1000 行，或已经混合多层职责时，应先评估封装边界。若属于上游同步、第三方或生成文件，优先保持同步友好，不做无关拆分。

## 代码风格

- Python 使用 4 个空格缩进，函数和变量使用 `snake_case`，类使用 `CamelCase`。
- 当前仓库通过 `flake8`、`isort` 和 `yapf` 执行静态检查与格式化，行宽上限为 120；具体配置以 `setup.cfg` 和 `.pre-commit-config.yaml` 为准。
- 不手工大范围调整导入和格式；让仓库已有工具处理本次涉及的文件。
- 注释用于解释动机、约束和不直观的逻辑，不复述显而易见的代码。
- 新增版权头或文件头时遵循相邻文件，不复制其他项目的作者、时间戳或业务说明。
- 不在日志、示例、测试或配置中写入令牌、密码、内部地址或真实敏感数据。

常用静态检查命令：

```bash
python -m pip install pre-commit
pre-commit run --all-files
```

优先执行与本次改动相关的 hook 或文件检查；准备提交前再视范围执行全量检查。运行仓库命令前应确认其依赖脚本在当前 checkout 中存在，不根据过期入口猜测可用性。

## 环境与依赖

- Python、PyTorch、Transformers 及可选后端版本以当前 `README_CN.md`、`setup.py` 和 `requirements/` 的共同约束为准；发现不一致时明确指出，不自行选择并宣称为唯一标准。
- 从源码进行基础开发可使用 `python -m pip install -e .`。只安装任务需要的可选依赖，不默认安装 `all` 依赖集。
- 未经用户同意，不升级或降级无关依赖，不修改锁定范围，不下载大型模型或数据集。
- 在 CPU 或缺少目标加速器的环境中，优先验证纯逻辑、参数解析、数据转换和轻量工具函数。
- 依赖 GPU/NPU、分布式运行、外部服务或大型权重的功能，应在匹配的环境中验证；无法执行时明确标记“未执行”并说明缺少的条件。

## 测试与验证

验证范围应与风险和影响面匹配：

1. 文档或配置修改：执行目标格式检查、链接或结构检查。
2. 纯工具逻辑：优先运行对应的 CPU 单元测试。
3. 模型、模板、数据集或参数修改：覆盖直接单元测试以及至少一条相关集成路径。
4. 训练、推理、导出或部署修改：验证最小可运行流程，并记录模型、设备、精度、关键参数和输入规模。
5. 公共接口或共享模块修改：扩大到受影响调用方的回归测试。

仓库测试以 `unittest` 和 `tests/run.py` 为主。可按文件或目录执行最小相关用例，例如：

```bash
python tests/run.py --test_dir tests/utils --pattern test_file_utils.py
python -m unittest tests.utils.test_file_utils
```

完整 CI 可能安装额外依赖、调用容器并使用加速设备，不应在普通本地修改中盲目执行。不得把“代码可导入”“命令启动成功”或 mock 测试通过等同于真实训练、推理或性能验证。

涉及随机过程时，根据测试目标固定随机种子。性能优化必须先建立基线，在相同硬件、输入和参数下复测；没有实测数据时不得声称性能提升。

## 文档与示例

- 用户可见行为、参数或默认值变化时，同步检查相关文档、示例和帮助文本。
- 示例应使用占位路径和非敏感数据，保持最小、可理解，并与当前接口一致。
- 中英文文档存在对应页面时，评估是否需要同步更新；若只修改一侧，应说明原因。
- 不把未复现的效果、硬件支持或兼容性写成已验证结论。

## Git 与交付

- 保留用户已有改动，不还原、不覆盖、不混入无关文件。
- 未经明确要求，不创建提交、不推送、不改写历史、不切换或删除分支。
- 完成前检查 `git diff --check`、实际差异和未跟踪文件，确认改动范围符合任务。
- 最终说明应包含：改了什么、为什么这样改、执行了哪些验证、哪些验证未执行及其原因、仍存在的限制或风险。

## 完成标准

任务不能仅以“代码已经写完”为完成。至少应满足：

- 实现符合已确认需求；
- 有与风险相匹配的验证证据；
- 没有引入无关改动或不必要复杂度；
- 公共接口、依赖和运行环境影响已被检查；
- 未验证事项与残余风险已明确说明。
